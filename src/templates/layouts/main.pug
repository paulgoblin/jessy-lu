doctype html
html(lang="en")
  head
    title
      block title
    meta(content="width=device-width, initial-scale=1",name="viewport")
    - var ts = Date.now();
    link(rel="stylesheet" href='styles.css?ts=' + ts)
    script(src='index.js?ts=' + ts).
  body
    .main-container#main-container
      block content
    block detailTemplates

